# Java Swing 수강신청 프로그램 - 클래스별 메서드 특징 및 사용처 문서

이 문서는 수강신청 프로그램의 모든 클래스와 각 메서드의 특징, 사용처, 매개변수, 반환값을 상세히 설명합니다.

---

## 목차

1. [Model 패키지](#1-model-패키지)
2. [DAO 패키지](#2-dao-패키지)
3. [Controller 패키지](#3-controller-패키지)
4. [View 패키지](#4-view-패키지)
5. [Utility 및 기타](#5-utility-및-기타)
6. [Constants 패키지](#6-constants-패키지)

---

## 1. Model 패키지

Model 패키지는 데이터 전송 객체(DTO)와 애플리케이션 상태 관리를 담당합니다.

### 1.1 MMain.java

**클래스 설명**: 애플리케이션의 전역 상태를 관리하는 모델 클래스로, 현재 로그인한 사용자 정보를 저장합니다.

#### 메서드 목록

##### `MMain()`
- **특징**: 기본 생성자
- **사용처**: 애플리케이션 시작 시 ClassConnector에서 인스턴스 생성
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**: currentUserId를 null로 초기화

##### `String getCurrentUserId()`
- **특징**: 현재 로그인한 사용자의 ID를 반환
- **사용처**: 모든 Controller에서 사용자 인증 상태 확인 시
- **매개변수**: 없음
- **반환값**: 
  - `String`: 현재 로그인한 사용자 ID
  - `null`: 로그인하지 않은 상태
- **사용 예시**: 
  ```java
  String userId = mMain.getCurrentUserId();
  if (userId == null) {
      // 로그인 필요 메시지 표시
  }
  ```

##### `void setCurrentUserId(String userId)`
- **특징**: 현재 사용자 ID를 설정하고 로그 기록
- **사용처**: 
  - 로그인 성공 시: CLogin.handleLogin()
  - 로그아웃 시: CMain.handleLogout()에서 null 전달
- **매개변수**: 
  - `userId` (String): 설정할 사용자 ID, null이면 로그아웃
- **반환값**: 없음
- **주요 동작**:
  - userId가 null이면 로그아웃 로그 기록 (WARNING 레벨)
  - userId가 유효하면 로그인 로그 기록 (INFO 레벨)

---

### 1.2 MUser.java

**클래스 설명**: 사용자 정보를 담는 DTO 클래스로, DB의 user 테이블과 매핑됩니다.

#### 필드 목록
- `userid` (String): 사용자 ID (PK)
- `name` (String): 사용자 이름
- `code` (int): 학번 (8자리)
- `email` (String): 이메일 주소
- `campusId` (int): 캠퍼스 ID (FK)
- `collegeId` (int): 단과대학 ID (FK)
- `departmentId` (int): 학과 ID (FK)
- `role` (String): 역할 (일반 사용자/관리자)
- `campus` (String): 캠퍼스 이름
- `college` (String): 단과대학 이름
- `department` (String): 학과 이름
- `password` (String): 비밀번호 (해시된 값)

#### 메서드 목록

##### `MUser()`
- **특징**: 기본 생성자, 모든 필드는 기본값으로 초기화
- **사용처**: JDBC ResultSet에서 데이터 매핑 시, 직렬화 프레임워크
- **매개변수**: 없음
- **반환값**: 없음

##### Getter/Setter 메서드들
각 필드마다 다음과 같은 패턴의 메서드가 있습니다:

- `String getUserid()` / `void setUserid(String userid)`
- `String getName()` / `void setName(String name)`
- `int getCode()` / `void setCode(int code)`
- `String getEmail()` / `void setEmail(String email)`
- `String getRole()` / `void setRole(String role)`
- `int getCampusId()` / `void setCampusId(int campusId)`
- `int getCollegeId()` / `void setCollegeId(int collegeId)`
- `int getDepartmentId()` / `void setDepartmentId(int departmentId)`
- `String getCampus()` / `void setCampus(String campus)`
- `String getCollege()` / `void setCollege(String college)`
- `String getDepartment()` / `void setDepartment(String department)`
- `String getPassword()` / `void setPassword(String password)`

**특징**: JavaBeans 규약을 따르는 표준 Getter/Setter 메서드
**사용처**: DAO에서 DB 데이터 매핑 시, Controller에서 사용자 정보 접근 시

---

### 1.3 MLecture.java

**클래스 설명**: 강의 정보를 담는 DTO 클래스로, DB의 lecture 테이블과 매핑됩니다.

#### 필드 목록
- `id` (String): 강의 코드 (PK)
- `name` (String): 강의명
- `professor` (String): 교수명
- `credits` (int): 학점
- `schedule` (String): 시간표 (예: "월1,2 수3,4")
- `deptId` (int): 학과 ID (FK)

#### 메서드 목록

##### `MLecture()`
- **특징**: 기본 생성자
- **사용처**: 직렬화 및 프레임워크 호환성
- **매개변수**: 없음
- **반환값**: 없음

##### `MLecture(String id, String name, String professor, int credits, String schedule)`
- **특징**: 주요 필드를 초기화하는 생성자 (deptId는 0으로 초기화)
- **사용처**: DAO에서 강의 목록 조회 시 객체 생성
- **매개변수**:
  - `id`: 강의 코드
  - `name`: 강의명
  - `professor`: 교수명
  - `credits`: 학점
  - `schedule`: 시간표
- **반환값**: 없음

##### `MLecture(String id, String name, String professor, int credits, String schedule, int deptId)`
- **특징**: 모든 필드를 초기화하는 생성자
- **사용처**: 관리자 화면에서 전체 강의 정보 조회 시
- **매개변수**: 위의 5개 + `deptId` (학과 ID)
- **반환값**: 없음

##### Getter 메서드들
- `String getId()`: 강의 코드 반환
- `String getName()`: 강의명 반환
- `String getProfessor()`: 교수명 반환
- `int getCredits()`: 학점 반환
- `String getSchedule()`: 시간표 반환
- `int getDeptId()`: 학과 ID 반환

**특징**: 읽기 전용 접근 제공 (Setter 없음)
**사용처**: View에서 테이블에 데이터 표시 시

---

### 1.4 ComboboxItem.java

**클래스 설명**: JComboBox에서 표시할 이름과 DB ID를 함께 담는 DTO 클래스입니다.

#### 필드 목록
- `name` (String): 화면에 표시될 이름
- `id` (int): DB에서 사용할 ID 값

#### 메서드 목록

##### `ComboboxItem(String name, int id)`
- **특징**: 이름과 ID를 함께 저장하는 생성자
- **사용처**: DAO에서 캠퍼스/대학/학과 목록 조회 후 객체 생성
- **매개변수**:
  - `name`: 표시할 이름 (예: "컴퓨터공학과")
  - `id`: DB ID 값 (예: 112)
- **반환값**: 없음

##### `String getName()`
- **특징**: 표시 이름 반환
- **사용처**: Controller에서 선택된 항목의 이름 확인 시
- **매개변수**: 없음
- **반환값**: 표시 이름 (String)

##### `int getId()`
- **특징**: DB ID 반환
- **사용처**: Controller에서 선택된 항목으로 DB 쿼리 실행 시
- **매개변수**: 없음
- **반환값**: DB ID (int)

##### `String toString()`
- **특징**: JComboBox에 표시될 문자열 반환 (Object 클래스 오버라이드)
- **사용처**: JComboBox가 자동으로 호출하여 항목 표시
- **매개변수**: 없음
- **반환값**: name 필드 값

**사용 예시**:
```java
ComboboxItem item = new ComboboxItem("공과대학", 11);
comboBox.addItem(item);  // JComboBox에 "공과대학" 표시
int selectedId = ((ComboboxItem) comboBox.getSelectedItem()).getId();  // 11 반환
```

---

## 2. DAO 패키지

DAO(Data Access Object) 패키지는 데이터베이스 접근 로직을 캡슐화합니다.

### 2.1 DAO.java

**클래스 설명**: 모든 DAO 클래스의 기반이 되는 DB 연결 관리 클래스입니다.

#### 필드 목록
- `DBURL` (String, static final): MySQL 접속 URL
- `DBID` (String, static final): DB 사용자 ID (root)
- `dbpassword` (String): config.properties에서 읽어온 DB 비밀번호
- `logger` (Logger, static final): 로그 기록용

#### 메서드 목록

##### `DAO()`
- **특징**: 생성자, DB 비밀번호 로드 및 JDBC 드라이버 로드
- **사용처**: 각 DAO 클래스(UserDAO, LectureDAO, SaveDAO) 생성 시
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. loadDbPassword() 호출하여 config.properties 읽기
  2. loadJdbcDriver() 호출하여 MySQL JDBC 드라이버 로드

##### `private void loadDbPassword()`
- **특징**: config.properties 파일에서 DB 비밀번호 읽기
- **사용처**: 생성자에서 자동 호출
- **매개변수**: 없음
- **반환값**: 없음
- **예외 처리**: IOException 발생 시 SEVERE 레벨 로그 기록
- **주의사항**: config.properties 파일이 프로젝트 루트에 있어야 함

##### `private void loadJdbcDriver()`
- **특징**: MySQL JDBC 드라이버 클래스 로드
- **사용처**: 생성자에서 자동 호출
- **매개변수**: 없음
- **반환값**: 없음
- **예외 처리**: ClassNotFoundException 발생 시 SEVERE 레벨 로그 기록

##### `Connection getConnection()`
- **특징**: MySQL 데이터베이스 연결 객체 생성 및 반환
- **사용처**: 모든 DAO 메서드에서 DB 작업 시작 전 호출
- **매개변수**: 없음
- **반환값**:
  - `Connection`: 성공 시 연결 객체
  - `null`: 연결 실패 시
- **주요 동작**: DriverManager.getConnection()을 통해 연결 생성
- **주의사항**: 
  - 호출한 곳에서 try-with-resources로 반드시 닫아야 함
  - null 반환 시 예외 처리 필요

##### `static void close(ResultSet rs, PreparedStatement pstmt, Connection conn)`
- **특징**: DB 리소스(ResultSet, PreparedStatement, Connection)를 안전하게 닫기
- **사용처**: DAO 메서드의 finally 블록 또는 catch 블록에서 호출
- **매개변수**:
  - `rs`: 닫을 ResultSet 객체 (null 가능)
  - `pstmt`: 닫을 PreparedStatement 객체 (null 가능)
  - `conn`: 닫을 Connection 객체 (null 가능)
- **반환값**: 없음
- **주요 동작**:
  - 각 리소스가 null이 아니고 닫히지 않았으면 close() 호출
  - 닫기 실패 시 WARNING 레벨 로그 기록
- **주의사항**: 순서대로 rs → pstmt → conn 순으로 닫음


---

### 2.2 UserDAO.java

**클래스 설명**: 사용자 인증, 회원가입, 정보 조회, 비밀번호 관리를 담당하는 DAO 클래스입니다. BCrypt를 사용한 비밀번호 해싱을 지원합니다.

#### 필드 목록
- `dao` (DAO): DB 연결을 위한 DAO 인스턴스
- `logger` (Logger): 로그 기록용
- `MIN_PASSWORD_HASH_LENGTH` (int, static final): 비밀번호 해시 최소 길이 (60)
- `TARGET_PASSWORD_COLUMN_LENGTH` (int, static final): 목표 비밀번호 컬럼 길이 (100)
- `passwordColumnChecked` (boolean, static volatile): 비밀번호 컬럼 길이 확인 여부

#### 메서드 목록

##### `UserDAO()`
- **특징**: 생성자, DAO 인스턴스 생성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 없음
- **반환값**: 없음

##### `MUser validateUser(String id, String password) throws SQLException`
- **특징**: 로그인 인증 처리 (BCrypt 해시 검증 및 평문 비밀번호 자동 마이그레이션)
- **사용처**: CLogin.handleLogin()에서 사용자 로그인 시
- **매개변수**:
  - `id`: 사용자 ID
  - `password`: 입력한 비밀번호 (평문)
- **반환값**:
  - `MUser`: 인증 성공 시 사용자 정보 (userid, name, role 포함)
  - `null`: 인증 실패 (아이디 또는 비밀번호 불일치)
- **예외**: `SQLException` - DB 연결 실패 또는 쿼리 오류
- **주요 동작**:
  1. DB에서 사용자의 해시된 비밀번호 조회
  2. BCrypt 해시인 경우 BCrypt.checkpw()로 검증
  3. 평문인 경우 직접 비교 후, 검증 성공 시 자동으로 해시로 변환
  4. 검증 성공 시 MUser 객체 반환
- **주의사항**:
  - 평문 비밀번호는 자동으로 BCrypt 해시로 마이그레이션됨
  - DB 연결 실패 시 SQLException 발생

##### `boolean addUser(MUser mUser, String password)`
- **특징**: 회원가입 처리 (user, login 테이블에 트랜잭션으로 INSERT)
- **사용처**: CSignup.handleSignup()에서 회원가입 완료 시
- **매개변수**:
  - `mUser`: 사용자 정보 객체 (userid, name, code, email, campus_id, college_id, department_id)
  - `password`: 비밀번호 평문 (자동으로 BCrypt 해싱됨)
- **반환값**:
  - `true`: 회원가입 성공
  - `false`: 회원가입 실패 (중복, 외래키 오류, DB 연결 실패)
- **주요 동작**:
  1. 트랜잭션 시작 (conn.setAutoCommit(false))
  2. user 테이블에 INSERT
  3. login 테이블에 INSERT (비밀번호는 BCrypt 해싱)
  4. 둘 다 성공 시 commit, 하나라도 실패 시 rollback
- **주의사항**:
  - 호출 전 isUserIdDuplicate()와 isStudentIdDuplicate()로 중복 검사 권장
  - 비밀번호는 BCrypt로 자동 해싱되어 저장됨

##### `boolean isUserIdDuplicate(String id)`
- **특징**: 사용자 ID 중복 검사
- **사용처**: CSignup.handleSignup()에서 회원가입 전 검사
- **매개변수**: `id` - 검사할 사용자 ID
- **반환값**:
  - `true`: 이미 존재하는 ID (사용 불가)
  - `false`: 사용 가능한 ID 또는 DB 오류
- **주요 동작**: user 테이블에서 COUNT(*) 쿼리 실행

##### `boolean isStudentIdDuplicate(int studentCode)`
- **특징**: 학번 중복 검사
- **사용처**: CSignup.handleSignup()에서 회원가입 전 검사
- **매개변수**: `studentCode` - 검사할 학번 (8자리 정수)
- **반환값**:
  - `true`: 이미 존재하는 학번 (사용 불가)
  - `false`: 사용 가능한 학번 또는 DB 오류
- **주요 동작**: user 테이블에서 code 컬럼 기준 COUNT(*) 쿼리 실행

##### `int getCampusIdByUserId(String userId)`
- **특징**: 사용자의 캠퍼스 ID 조회
- **사용처**: CSearch.loadInitialCollegeData()에서 사용자 소속 캠퍼스의 대학 목록 로드 시
- **매개변수**: `userId` - 사용자 ID
- **반환값**:
  - 양수: 캠퍼스 ID
  - `-1`: 사용자를 찾지 못했거나 DB 오류
- **주요 동작**: user 테이블에서 campus_id 컬럼 조회

##### `MUser getUserInfo(String userId)`
- **특징**: 사용자 상세 정보 조회 (소속 기관명 포함)
- **사용처**: CMain.handleMyInfo()에서 내 정보 표시 시
- **매개변수**: `userId` - 사용자 ID
- **반환값**:
  - `MUser`: 사용자 정보 (userid, name, code, email, 캠퍼스명, 대학명, 학과명)
  - `null`: 사용자를 찾지 못했거나 DB 오류
- **주요 동작**: user, root, college, department 테이블 JOIN 쿼리로 전체 정보 조회

##### `boolean resetPasswordIfMatch(String id, String name, int code, String newPassword) throws SQLException`
- **특징**: 사용자 정보 일치 시 비밀번호 초기화
- **사용처**: CLogin.handleResetPassword()에서 비밀번호 찾기 기능
- **매개변수**:
  - `id`: 사용자 ID
  - `name`: 이름
  - `code`: 학번
  - `newPassword`: 새 비밀번호 (평문, BCrypt로 해싱됨)
- **반환값**:
  - `true`: 비밀번호 초기화 성공
  - `false`: 일치하는 사용자 없음
- **예외**: `SQLException` - DB 오류
- **주요 동작**:
  1. user 테이블에서 id, name, code가 모두 일치하는지 확인
  2. 일치하면 login 테이블의 password를 새 비밀번호(BCrypt 해시)로 업데이트

##### `boolean changePassword(String userId, String oldPassword, String newPassword) throws SQLException`
- **특징**: 현재 비밀번호 확인 후 새 비밀번호로 변경
- **사용처**: 비밀번호 변경 기능 (추후 구현 시 사용)
- **매개변수**:
  - `userId`: 사용자 ID
  - `oldPassword`: 현재 비밀번호 (평문)
  - `newPassword`: 새 비밀번호 (평문, BCrypt로 해싱됨)
- **반환값**:
  - `true`: 비밀번호 변경 성공
  - `false`: 현재 비밀번호 불일치 또는 업데이트 실패
- **예외**: `SQLException` - DB 오류
- **주요 동작**:
  1. login 테이블에서 현재 비밀번호 해시 조회
  2. BCrypt.checkpw()로 현재 비밀번호 검증
  3. 검증 성공 시 새 비밀번호(BCrypt 해시)로 업데이트

##### `private boolean updateLoginPassword(String userId, String newPassword, Connection connection) throws SQLException`
- **특징**: login 테이블의 비밀번호 업데이트 (내부 헬퍼 메서드)
- **사용처**: resetPasswordIfMatch(), changePassword()에서 호출
- **매개변수**:
  - `userId`: 사용자 ID
  - `newPassword`: 새 비밀번호 (평문, BCrypt로 해싱됨)
  - `connection`: 기존 Connection 객체 (트랜잭션 유지용)
- **반환값**:
  - `true`: 업데이트 성공
  - `false`: 업데이트 실패
- **예외**: `SQLException` - DB 오류

##### `private void ensurePasswordColumnLength(Connection connection)`
- **특징**: login 테이블의 password 컬럼 길이가 BCrypt 해시에 충분한지 확인하고, 부족하면 자동으로 확장
- **사용처**: validateUser(), addUser(), resetPasswordIfMatch(), changePassword() 내부에서 자동 호출
- **매개변수**: `connection` - DB 연결 객체
- **반환값**: 없음
- **주요 동작**:
  1. SHOW COLUMNS 쿼리로 password 컬럼 길이 확인
  2. 60자 미만이면 VARCHAR(100)으로 ALTER TABLE 실행
  3. 확인 완료 후 passwordColumnChecked를 true로 설정 (중복 실행 방지)
- **주의사항**: 멀티스레드 환경을 고려하여 synchronized 블록 사용

##### `private void migratePlaintextPassword(String id, String password, Connection connection)`
- **특징**: 평문 비밀번호를 BCrypt 해시로 자동 마이그레이션
- **사용처**: validateUser() 내부에서 평문 비밀번호 검증 성공 시 자동 호출
- **매개변수**:
  - `id`: 사용자 ID
  - `password`: 평문 비밀번호
  - `connection`: DB 연결 객체
- **반환값**: 없음
- **주요 동작**: BCrypt.hashpw()로 해싱 후 login 테이블 업데이트

##### `private void rollbackQuietly(Connection conn)`
- **특징**: 트랜잭션 롤백 (예외 발생 시 로그만 기록)
- **사용처**: addUser()의 예외 처리 블록
- **매개변수**: `conn` - Connection 객체
- **반환값**: 없음

##### `private void restoreAutoCommit(Connection conn)`
- **특징**: AutoCommit 모드를 true로 복구
- **사용처**: addUser()의 finally 블록
- **매개변수**: `conn` - Connection 객체
- **반환값**: 없음

---

### 2.3 LectureDAO.java

**클래스 설명**: 강의 정보 조회, CRUD, 계층적 소속 정보(캠퍼스/대학/학과) 조회를 담당하는 DAO 클래스입니다.

#### 필드 목록
- `dao` (DAO): DB 연결을 위한 DAO 인스턴스
- `logger` (Logger): 로그 기록용

#### 메서드 목록

##### `LectureDAO()`
- **특징**: 생성자, DAO 인스턴스 생성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 없음
- **반환값**: 없음

##### `List<ComboboxItem> getAllCampuses()`
- **특징**: 전체 캠퍼스 목록 조회
- **사용처**: CSignup.loadInitialCampusData()에서 회원가입 시 캠퍼스 콤보박스 초기화
- **매개변수**: 없음
- **반환값**: `List<ComboboxItem>` - 캠퍼스 목록 (빈 리스트는 DB 오류를 의미)
- **주요 동작**: root 테이블에서 id, name 조회 후 ComboboxItem 리스트 생성
- **SQL**: `SELECT id, name FROM root ORDER BY id`

##### `List<ComboboxItem> getCollegesByCampus(int campusId)`
- **특징**: 특정 캠퍼스의 단과대학 목록 조회 (교양 제외)
- **사용처**: 
  - CSignup.handleCampusSelect()에서 캠퍼스 선택 시
  - CSearch.loadInitialCollegeData()에서 로그인 후 대학 목록 로드
- **매개변수**: `campusId` - 조회할 캠퍼스 ID
- **반환값**: `List<ComboboxItem>` - 단과대학 목록
- **주요 동작**: college 테이블에서 root_id가 일치하고 name이 '교양'이 아닌 레코드 조회
- **SQL**: `SELECT c.id, c.name FROM college c WHERE c.root_id = ? AND c.name != '교양' ORDER BY c.id`

##### `List<ComboboxItem> getDepartmentsByCollege(int collegeId)`
- **특징**: 특정 단과대학의 학과 목록 조회
- **사용처**: 
  - CSignup.handleCollegeSelect()에서 대학 선택 시
  - CSearch.handleCollegeSelect()에서 대학 선택 시
- **매개변수**: `collegeId` - 조회할 단과대학 ID
- **반환값**: `List<ComboboxItem>` - 학과 목록
- **주요 동작**: department 테이블에서 college_id가 일치하는 레코드 조회
- **SQL**: `SELECT d.id, d.name FROM department d WHERE d.college_id = ? ORDER BY d.id`

##### `List<MLecture> searchLectures(String userId, String collegeName, String deptName, String keyword)`
- **특징**: 사용자의 캠퍼스 내에서 강의 검색 (대학/학과/키워드 필터링)
- **사용처**: CSearch.handleSearch()에서 강의 검색 시
- **매개변수**:
  - `userId`: 사용자 ID (캠퍼스 필터링용)
  - `collegeName`: 대학 이름 (null 또는 빈 문자열이면 전체)
  - `deptName`: 학과 이름 (null 또는 빈 문자열이면 전체)
  - `keyword`: 검색 키워드 (강의명, 교수명에서 검색, 공백 무시)
- **반환값**: `List<MLecture>` - 검색된 강의 목록
- **주요 동작**:
  1. lecture, department, college, root, user 테이블 JOIN
  2. 사용자의 캠퍼스와 일치하는 강의만 조회
  3. 대학, 학과, 키워드 필터 동적 적용
  4. 키워드는 공백 제거 후 LIKE 검색 (강의명, 교수명)
- **주의사항**: 동적 SQL 생성이므로 PreparedStatement로 SQL 인젝션 방지

##### `List<MLecture> getAllLectures()`
- **특징**: 전체 강의 목록 조회 (관리자용)
- **사용처**: CAdmin.refreshTable()에서 관리자 화면 초기화 시
- **매개변수**: 없음
- **반환값**: `List<MLecture>` - 전체 강의 목록 (department_id 포함)
- **주요 동작**: lecture 테이블의 모든 레코드 조회
- **SQL**: `SELECT id, name, professor, credit, time, department_id FROM lecture ORDER BY id`

##### `List<String> getAllDepartments()`
- **특징**: 전체 학과 목록 조회 (ID, 학과명, 소속 대학명 형식)
- **사용처**: CAdmin에서 "학과 찾기" 버튼 클릭 시
- **매개변수**: 없음
- **반환값**: `List<String>` - 형식: "112: 컴퓨터공학과 (공과대학)"
- **주요 동작**: department와 college 테이블 JOIN 후 포맷팅
- **SQL**: `SELECT d.id, d.name, c.name AS college_name FROM department d JOIN college c ON d.college_id = c.id ORDER BY d.id`

##### `boolean insertLecture(int id, String name, String prof, int credit, String time, int deptId)`
- **특징**: 새 강의 추가 (관리자용)
- **사용처**: CAdmin.handleAdd()에서 강의 추가 버튼 클릭 시
- **매개변수**:
  - `id`: 강의 코드 (PK)
  - `name`: 강의명
  - `prof`: 교수명
  - `credit`: 학점
  - `time`: 시간표 (예: "월1,2 수3,4")
  - `deptId`: 학과 ID (FK)
- **반환값**:
  - `true`: 추가 성공
  - `false`: 추가 실패 (중복 ID, 외래키 오류, DB 연결 실패)
- **SQL**: `INSERT INTO lecture (id, name, professor, credit, time, department_id) VALUES (?, ?, ?, ?, ?, ?)`

##### `boolean updateLecture(int id, String name, String prof, int credit, String time, int deptId)`
- **특징**: 기존 강의 정보 수정 (관리자용)
- **사용처**: CAdmin.handleUpdate()에서 강의 수정 버튼 클릭 시
- **매개변수**: insertLecture()와 동일
- **반환값**:
  - `true`: 수정 성공
  - `false`: 수정 실패 (해당 ID 없음, DB 오류)
- **SQL**: `UPDATE lecture SET name=?, professor=?, credit=?, time=?, department_id=? WHERE id=?`
- **주의사항**: id(PK)는 수정 불가

##### `boolean deleteLecture(int id)`
- **특징**: 강의 삭제 (관리자용)
- **사용처**: CAdmin.handleDelete()에서 강의 삭제 버튼 클릭 시
- **매개변수**: `id` - 삭제할 강의 코드
- **반환값**:
  - `true`: 삭제 성공
  - `false`: 삭제 실패 (해당 ID 없음, 외래키 제약, DB 오류)
- **SQL**: `DELETE FROM lecture WHERE id = ?`
- **주의사항**: save 테이블에서 참조 중이면 외래키 제약으로 삭제 실패 가능


---

### 2.4 SaveDAO.java

**클래스 설명**: 수강신청 및 미리담기 관련 데이터 조회, 추가, 삭제, 학점 계산을 담당하는 DAO 클래스입니다.

#### 필드 목록
- `dao` (DAO): DB 연결을 위한 DAO 인스턴스
- `logger` (Logger): 로그 기록용

#### 메서드 목록

##### `SaveDAO()`
- **특징**: 생성자, DAO 인스턴스 생성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 없음
- **반환값**: 없음

##### `List<MLecture> getLecturesByStatus(String userid, String status)`
- **특징**: 특정 상태(수강신청/미리담기)의 강의 목록 조회
- **사용처**: 
  - CRegister.refreshTable()에서 수강신청 목록 조회
  - CPreRegister.refreshTable()에서 미리담기 목록 조회
  - CSchedule에서 시간표 데이터 로드
- **매개변수**:
  - `userid`: 사용자 ID
  - `status`: 상태 (StatusConstants.REGISTER 또는 PRE_REGISTER)
- **반환값**: `List<MLecture>` - 강의 목록 (오류 시 빈 리스트)
- **주요 동작**: lecture와 save 테이블 JOIN 후 userid와 status로 필터링
- **SQL**: `SELECT l.id, l.name, l.professor, l.credit, l.time FROM lecture l JOIN save s ON l.id = s.lecture_id WHERE s.userid = ? AND s.status = ?`

##### `int addLecture(String userid, int lectureid, String status, int newCredits)`
- **특징**: 수강신청 또는 미리담기에 강의 추가 (학점 검사 포함)
- **사용처**: CSearch.handleSave()에서 수강신청/미리담기 버튼 클릭 시
- **매개변수**:
  - `userid`: 사용자 ID
  - `lectureid`: 추가할 강의 코드
  - `status`: 상태 (StatusConstants.REGISTER 또는 PRE_REGISTER)
  - `newCredits`: 추가할 강의의 학점
- **반환값**:
  - `AppConstants.DB_SUCCESS (0)`: 정상 추가
  - `AppConstants.DB_ERROR_CREDIT_EXCEEDED (1)`: 학점 초과 (18학점)
  - `AppConstants.DB_ERROR_DUPLICATE (2)`: 이미 추가된 강의
  - `AppConstants.DB_ERROR_GENERAL (-1)`: DB 오류
- **주요 동작**:
  1. status가 REGISTER인 경우만 학점 검사 수행
  2. 현재 학점 + 새 학점 > MAX_CREDITS(18)이면 DB_ERROR_CREDIT_EXCEEDED 반환
  3. INSERT IGNORE 사용으로 중복 시 자동 무시
  4. 삽입된 행 수로 성공/중복 판단
- **SQL**: `INSERT IGNORE INTO save (userid, lecture_id, status) VALUES (?, ?, ?)`

##### `boolean removeLecture(String userid, int lectureid, String status)`
- **특징**: 수강신청 또는 미리담기에서 강의 삭제
- **사용처**: 
  - CRegister.handleDelete()에서 신청 취소 시
  - CPreRegister.handleDelete()에서 미리담기 삭제 시
- **매개변수**:
  - `userid`: 사용자 ID
  - `lectureid`: 삭제할 강의 코드
  - `status`: 상태 (StatusConstants.REGISTER 또는 PRE_REGISTER)
- **반환값**:
  - `true`: 삭제 성공
  - `false`: 삭제 실패 (해당 데이터 없음, DB 연결 실패)
- **주요 동작**: save 테이블에서 userid, lecture_id, status가 모두 일치하는 레코드 삭제
- **SQL**: `DELETE FROM save WHERE userid = ? AND lecture_id = ? AND status = ?`

##### `int getTotalCredits(String userid, String status)`
- **특징**: 특정 상태(수강신청/미리담기)의 총 학점 계산
- **사용처**:
  - addLecture() 내부에서 학점 초과 검사 시
  - CRegister/CPreRegister에서 총 학점 표시 시
- **매개변수**:
  - `userid`: 사용자 ID
  - `status`: 상태 (StatusConstants.REGISTER 또는 PRE_REGISTER)
- **반환값**:
  - 양수: 총 학점
  - `0`: 신청한 강의 없음 또는 DB 오류
- **주요 동작**: lecture와 save 테이블 JOIN 후 SUM(credit) 계산
- **SQL**: `SELECT SUM(l.credit) AS total FROM lecture l JOIN save s ON l.id = s.lecture_id WHERE s.userid = ? AND s.status = ?`
- **주의사항**: SUM 결과가 NULL이면 0 반환

---

## 3. Controller 패키지

Controller 패키지는 사용자 입력을 처리하고 Model과 View를 연결합니다.

### 3.1 CMain.java

**클래스 설명**: 메인 화면의 네비게이션, 로그아웃, 테마 변경, 사용자 정보 표시 등 전반적인 흐름을 제어하는 메인 컨트롤러입니다.

#### 필드 목록
- `vMain` (VMain): 메인 뷰
- `mMain` (MMain): 메인 모델
- `userDAO` (UserDAO): 사용자 DAO
- `cSearch, cRegister, cPreRegister, cSchedule, cAdmin`: 각 기능별 컨트롤러
- `previousStack, forwardStack` (Deque<String>): 네비게이션 히스토리
- `currentPanel` (String): 현재 표시 중인 패널 이름
- `logger` (Logger): 로그 기록용

#### 메서드 목록

##### `CMain(VMain vMain, MMain mMain, UserDAO userDAO, ControllerBundle controllers)`
- **특징**: 생성자, 모든 컨트롤러와 뷰/모델 연결 및 이벤트 리스너 등록
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**:
  - `vMain`: 메인 뷰
  - `mMain`: 메인 모델
  - `userDAO`: 사용자 DAO
  - `controllers`: 각 기능별 컨트롤러 번들
- **반환값**: 없음
- **주요 동작**:
  1. 모든 필드 초기화
  2. 네비게이션 스택 초기화
  3. 메뉴 버튼, 네비게이션 버튼 리스너 등록
  4. 사이드바 메뉴 버튼 리스너 등록

##### `void resetNavigation(String panelName)`
- **특징**: 네비게이션 히스토리 초기화 및 특정 패널로 이동
- **사용처**: CLogin.handleLogin()에서 로그인 성공 후 초기 화면 설정
- **매개변수**: `panelName` - 이동할 패널 이름
- **반환값**: 없음
- **주요 동작**:
  1. 히스토리 스택 클리어
  2. currentPanel 설정
  3. 해당 패널 표시
  4. 네비게이션 버튼 상태 업데이트

##### `void setAdminMode(boolean isAdmin)`
- **특징**: 관리자 모드 설정/해제
- **사용처**: CLogin.handleLogin()에서 로그인 시 사용자 역할에 따라 호출
- **매개변수**: `isAdmin` - 관리자 여부 (true: 관리자 메뉴 표시)
- **반환값**: 없음
- **주요 동작**: 사이드바의 "관리자" 버튼 표시/숨김

##### `private void navigateTo(String panelName)`
- **특징**: 특정 패널로 이동하면서 히스토리 관리
- **사용처**: 사이드바 메뉴 버튼 클릭 시
- **매개변수**: `panelName` - 이동할 패널 이름
- **반환값**: 없음
- **주요 동작**:
  1. 로그인 패널로는 이동 불가
  2. 현재 패널을 previousStack에 push
  3. forwardStack 클리어
  4. 새 패널로 이동
  5. 네비게이션 버튼 상태 업데이트

##### `private void handlePrevious(ActionEvent e)`
- **특징**: 이전 버튼 클릭 처리
- **사용처**: 상단 툴바의 이전 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. previousStack에서 이전 패널 pop
  2. 현재 패널을 forwardStack에 push
  3. 이전 패널로 이동

##### `private void handleNext(ActionEvent e)`
- **특징**: 다음 버튼 클릭 처리
- **사용처**: 상단 툴바의 다음 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. forwardStack에서 다음 패널 pop
  2. 현재 패널을 previousStack에 push
  3. 다음 패널로 이동

##### `private void handleRefresh(ActionEvent e)`
- **특징**: 새로고침 버튼 클릭 처리
- **사용처**: 상단 툴바의 새로고침 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**: 현재 패널에 따라 적절한 컨트롤러의 refreshTable() 호출

##### `private void handleLogout(ActionEvent e)`
- **특징**: 로그아웃 처리
- **사용처**: 상단 툴바의 로그아웃 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. currentUserId를 null로 설정
  2. 관리자 모드 해제
  3. 로그인 화면으로 이동
  4. 창 크기를 로그인 크기(420x320)로 변경
  5. 사용자 이름 표시 초기화

##### `private void handleMyInfo(ActionEvent e)`
- **특징**: 내 정보 조회 및 표시
- **사용처**: 사이드바의 "내 정보" 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. UserDAO.getUserInfo()로 사용자 정보 조회
  2. HTML 형식으로 정보 포맷팅
  3. JOptionPane으로 팝업 표시

##### `private void handleThemeChange(ActionEvent e)`
- **특징**: 라이트/다크 테마 전환
- **사용처**: 사이드바의 "테마 변경" 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 현재 테마 확인
  2. FlatLightLaf ↔ FlatDarkLaf 전환
  3. FlatLaf.updateUI()로 전체 UI 업데이트

##### `public void refreshUserInfo()`
- **특징**: 헤더에 사용자 이름 표시
- **사용처**: CLogin.handleLogin()에서 로그인 후 호출
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. UserDAO.getUserInfo()로 사용자 정보 조회
  2. VMain의 사용자 이름 라벨 업데이트

##### `private void updateNavigationButtons()`
- **특징**: 네비게이션 버튼 활성화/비활성화 상태 업데이트
- **사용처**: 패널 이동 시마다 내부적으로 호출
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  - 이전 버튼: previousStack이 비어있으면 비활성화
  - 다음 버튼: forwardStack이 비어있으면 비활성화

---

### 3.2 CLogin.java

**클래스 설명**: 로그인, 회원가입 화면 전환, 비밀번호 초기화를 처리하는 컨트롤러입니다.

#### 메서드 목록 (주요 메서드)

##### `CLogin(VMain vMain, VLogin vLogin, MMain mMain, UserDAO userDAO, CSearch cSearch)`
- **특징**: 생성자, 로그인 화면 이벤트 리스너 등록
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 메인 뷰, 로그인 뷰, 메인 모델, 사용자 DAO, 검색 컨트롤러
- **반환값**: 없음
- **주요 동작**:
  1. 로그인 버튼 리스너 등록
  2. 회원가입 버튼 리스너 등록
  3. ID/PW 필드에 Enter 키 리스너 등록
  4. 비밀번호 초기화 버튼 리스너 등록

##### `private void handleLogin(ActionEvent e)`
- **특징**: 로그인 처리
- **사용처**: 로그인 버튼 클릭 또는 Enter 키 입력 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. ID/PW 입력값 읽기
  2. UserDAO.validateUser()로 인증
  3. 성공 시: 사용자 ID 설정, 관리자 모드 확인, 환영 메시지, 화면 전환
  4. 실패 시: 오류 메시지, PW 필드 초기화
  5. 항상: PW char 배열을 0으로 덮어 보안 강화
- **주의사항**: 
  - SQLException 발생 시 사용자에게 안내 후 로그 기록
  - 관리자 역할이면 관리자 모드 활성화

##### `private void handleResetPassword(ActionEvent e)`
- **특징**: 비밀번호 초기화 처리
- **사용처**: 비밀번호 초기화 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 다이얼로그로 ID, 이름, 학번 입력 받기
  2. 학번 형식 검증 (숫자 여부)
  3. config.properties에서 초기 비밀번호 읽기
  4. UserDAO.resetPasswordIfMatch() 호출
  5. 성공 시 완료 메시지, 실패 시 오류 메시지
- **주의사항**: SQLException 발생 시 로그 기록 및 사용자 안내

##### `void setCMain(CMain cMain)`
- **특징**: 메인 컨트롤러 설정 (순환 참조 방지용 세터)
- **사용처**: ClassConnector에서 모든 컨트롤러 생성 후 호출
- **매개변수**: `cMain` - 메인 컨트롤러
- **반환값**: 없음

---

### 3.3 CSignup.java

**클래스 설명**: 회원가입 처리, 입력 유효성 검사, 연쇄 콤보박스 관리를 담당하는 컨트롤러입니다.

#### 필드 목록 (주요 필드)
- Pattern 상수들: 비밀번호 검증용 정규표현식 (미리 컴파일됨)
  - `HAS_LETTER_PATTERN`: 영문자 포함 여부
  - `HAS_DIGIT_PATTERN`: 숫자 포함 여부
  - `HAS_SPECIAL_PATTERN`: 특수문자 포함 여부
  - `HAS_KOREAN_PATTERN`: 한글 포함 여부 (금지)
  - `STUDENT_CODE_PATTERN`: 학번 형식 (8자리 숫자)

#### 메서드 목록 (주요 메서드)

##### `CSignup(VMain vMain, VSignup vSignup, LectureDAO lectureDAO, UserDAO userDAO)`
- **특징**: 생성자, 회원가입 화면 이벤트 리스너 등록 및 초기 데이터 로드
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 메인 뷰, 회원가입 뷰, 강의 DAO, 사용자 DAO
- **반환값**: 없음
- **주요 동작**:
  1. 회원가입/취소 버튼 리스너 등록
  2. 캠퍼스/대학 콤보박스 리스너 등록
  3. 백그라운드 스레드로 캠퍼스 목록 로드

##### `private void loadInitialCampusData()`
- **특징**: 캠퍼스 목록 초기 로드 (백그라운드 스레드에서 실행)
- **사용처**: 생성자에서 new Thread()로 실행
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. LectureDAO.getAllCampuses() 호출
  2. 콤보박스 초기화 및 캠퍼스 항목 추가

##### `private void handleCampusSelect(ActionEvent e)`
- **특징**: 캠퍼스 선택 시 단과대학 목록 동적 로드
- **사용처**: 캠퍼스 콤보박스 선택 변경 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 선택된 캠퍼스의 ID 추출
  2. LectureDAO.getCollegesByCampus() 호출
  3. 대학 콤보박스 업데이트, 활성화
  4. 학과 콤보박스 초기화, 비활성화

##### `private void handleCollegeSelect(ActionEvent e)`
- **특징**: 단과대학 선택 시 학과 목록 동적 로드
- **사용처**: 대학 콤보박스 선택 변경 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 선택된 대학의 ID 추출
  2. LectureDAO.getDepartmentsByCollege() 호출
  3. 학과 콤보박스 업데이트, 활성화

##### `private void handleSignup(ActionEvent e)`
- **특징**: 회원가입 처리 (입력 검증 및 DB 저장)
- **사용처**: 회원가입 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 모든 입력 필드 값 읽기
  2. 필수 입력 검증 (빈 값 체크)
  3. 이메일 형식 검증 (정규표현식)
  4. 학번 형식 검증 (8자리 숫자)
  5. 아이디 길이 검증 (3~15자)
  6. 비밀번호 검증:
     - 길이: 8~20자
     - 영문자, 숫자, 특수문자 각 1개 이상 포함
     - 한글, 공백 금지
  7. 비밀번호 확인 일치 검사
  8. 중복 검사 (ID, 학번)
  9. UserDAO.addUser() 호출
  10. 성공 시 완료 메시지 및 로그인 화면 이동
- **주의사항**:
  - Pattern 객체는 미리 컴파일되어 ReDoS 공격 방지
  - 비밀번호는 BCrypt로 자동 해싱됨

##### `private void handleCancel(ActionEvent e)`
- **특징**: 회원가입 취소 및 로그인 화면 복귀
- **사용처**: 취소 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 입력 필드 초기화
  2. 창 크기를 로그인 크기로 변경
  3. 로그인 패널로 전환


---

### 3.4 CSearch.java

**클래스 설명**: 강의 검색 및 수강신청/미리담기 처리를 담당하는 컨트롤러입니다.

#### 메서드 목록 (주요 메서드)

##### `CSearch(VSearch vSearch, MMain mMain, LectureDAO lectureDAO, SaveDAO saveDAO, UserDAO userDAO)`
- **특징**: 생성자, 검색 화면 이벤트 리스너 등록
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 검색 뷰, 메인 모델, 강의 DAO, 저장 DAO, 사용자 DAO
- **반환값**: 없음
- **주요 동작**:
  1. 검색 버튼 리스너 등록
  2. 수강신청 버튼 리스너 등록
  3. 미리담기 버튼 리스너 등록
  4. 대학 콤보박스 리스너 등록

##### `void loadInitialCollegeData()`
- **특징**: 사용자 캠퍼스의 대학 목록 로드
- **사용처**: CLogin.handleLogin()에서 로그인 성공 후 호출
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. 현재 사용자 ID 확인
  2. UserDAO.getCampusIdByUserId()로 캠퍼스 ID 조회
  3. LectureDAO.getCollegesByCampus()로 대학 목록 조회
  4. 대학 콤보박스 업데이트

##### `private void handleCollegeSelect(ActionEvent e)`
- **특징**: 대학 선택 시 학과 목록 동적 로드
- **사용처**: 대학 콤보박스 선택 변경 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 선택된 대학의 ID 추출
  2. LectureDAO.getDepartmentsByCollege() 호출
  3. 학과 콤보박스 업데이트, 활성화

##### `private void handleSearch(ActionEvent e)`
- **특징**: 강의 검색 처리
- **사용처**: 검색 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 로그인 상태 확인
  2. 대학, 학과, 키워드 입력값 읽기
  3. LectureDAO.searchLectures() 호출
  4. 검색 결과를 테이블에 표시
  5. 결과 없으면 안내 메시지

##### `private void handleSave(String status)`
- **특징**: 수강신청 또는 미리담기 처리
- **사용처**: 
  - 수강신청 버튼 클릭 시 (status = StatusConstants.REGISTER)
  - 미리담기 버튼 클릭 시 (status = StatusConstants.PRE_REGISTER)
- **매개변수**: `status` - 저장 상태 (REGISTER 또는 PRE_REGISTER)
- **반환값**: 없음
- **주요 동작**:
  1. 로그인 상태 확인
  2. 테이블에서 선택된 강의 확인
  3. 강의 ID, 학점 추출
  4. SaveDAO.addLecture() 호출
  5. 결과 코드에 따라 적절한 메시지 표시:
     - DB_SUCCESS: 성공 메시지
     - DB_ERROR_CREDIT_EXCEEDED: 학점 초과 안내
     - DB_ERROR_DUPLICATE: 중복 안내
     - DB_ERROR_GENERAL: DB 오류 안내

##### `void setMode(String mode)`
- **특징**: 검색 모드 설정 (REGISTER/PREREGISTER)
- **사용처**: CMain의 사이드바 메뉴 버튼 클릭 시
- **매개변수**: `mode` - 검색 모드
- **반환값**: 없음
- **주요 동작**: 내부 mode 필드 업데이트 (향후 확장용)

---

### 3.5 CRegister.java

**클래스 설명**: 수강신청 내역 조회 및 취소를 처리하는 컨트롤러입니다. CListController를 상속받아 공통 로직을 재사용합니다.

#### 메서드 목록 (주요 메서드)

##### `CRegister(VRegister vRegister, MMain mMain, SaveDAO saveDAO)`
- **특징**: 생성자, 수강신청 화면 이벤트 리스너 등록
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 수강신청 뷰, 메인 모델, 저장 DAO
- **반환값**: 없음
- **주요 동작**:
  1. 부모 클래스(CListController) 생성자 호출 (status = REGISTER)
  2. 신청 취소 버튼 리스너 등록

##### `void refreshTable()`
- **특징**: 수강신청 목록 새로고침
- **사용처**: 
  - 로그인 후 초기 로드 시
  - 강의 추가/삭제 후
  - 새로고침 버튼 클릭 시
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. 현재 사용자 ID 확인
  2. SaveDAO.getLecturesByStatus(userId, REGISTER) 호출
  3. 테이블 업데이트
  4. 총 학점 계산 및 표시 (상속받은 메서드 활용)

##### `protected void updateViewTable(List<MLecture> lectureList)`
- **특징**: 수강신청 테이블 UI 업데이트 (추상 메서드 구현)
- **사용처**: refreshTable() 내부에서 호출
- **매개변수**: `lectureList` - 표시할 강의 목록
- **반환값**: 없음
- **주요 동작**:
  1. 테이블 모델 초기화
  2. 각 강의를 행으로 추가 (강의 코드, 이름, 교수, 학점, 시간)
  3. SaveDAO.getTotalCredits()로 총 학점 계산
  4. 총 학점 라벨 업데이트

---

### 3.6 CPreRegister.java

**클래스 설명**: 미리담기 내역 조회, 삭제, 수강신청 전환을 처리하는 컨트롤러입니다. CListController를 상속받아 공통 로직을 재사용합니다.

#### 메서드 목록 (주요 메서드)

##### `CPreRegister(VPreRegister vPreRegister, MMain mMain, SaveDAO saveDAO)`
- **특징**: 생성자, 미리담기 화면 이벤트 리스너 등록
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 미리담기 뷰, 메인 모델, 저장 DAO
- **반환값**: 없음
- **주요 동작**:
  1. 부모 클래스(CListController) 생성자 호출 (status = PRE_REGISTER)
  2. 삭제 버튼 리스너 등록
  3. 수강신청 전환 버튼 리스너 등록

##### `void refreshTable()`
- **특징**: 미리담기 목록 새로고침
- **사용처**: 
  - 로그인 후 초기 로드 시
  - 강의 추가/삭제 후
  - 새로고침 버튼 클릭 시
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. 현재 사용자 ID 확인
  2. SaveDAO.getLecturesByStatus(userId, PRE_REGISTER) 호출
  3. 테이블 업데이트
  4. 총 학점 계산 및 표시

##### `protected void updateViewTable(List<MLecture> lectureList)`
- **특징**: 미리담기 테이블 UI 업데이트 (추상 메서드 구현)
- **사용처**: refreshTable() 내부에서 호출
- **매개변수**: `lectureList` - 표시할 강의 목록
- **반환값**: 없음
- **주요 동작**: CRegister와 동일 (테이블 업데이트 및 총 학점 표시)

##### `private void handleMoveToRegister(ActionEvent e)`
- **특징**: 미리담기 → 수강신청 전환 처리
- **사용처**: "수강신청" 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 로그인 상태 확인
  2. 테이블에서 선택된 강의 확인
  3. 강의 ID, 학점 추출
  4. SaveDAO.addLecture(userId, lectureId, REGISTER, credits) 호출
  5. 추가 성공 시:
     - SaveDAO.removeLecture(userId, lectureId, PRE_REGISTER) 호출
     - 미리담기에서 제거
     - 성공 메시지 표시
     - 테이블 새로고침
  6. 추가 실패 시 (학점 초과, 중복 등): 적절한 메시지 표시

---

### 3.7 CSchedule.java

**클래스 설명**: 시간표 조회 및 이미지 저장을 처리하는 컨트롤러입니다.

#### 메서드 목록 (주요 메서드)

##### `CSchedule(VSchedule vSchedule, MMain mMain, SaveDAO saveDAO)`
- **특징**: 생성자, 시간표 화면 이벤트 리스너 등록
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 시간표 뷰, 메인 모델, 저장 DAO
- **반환값**: 없음
- **주요 동작**:
  1. 모드 선택 라디오 버튼 리스너 등록 (수강신청/미리담기)
  2. 이미지 저장 버튼 리스너 등록

##### `void showSchedule()`
- **특징**: 시간표 다이얼로그 표시 및 데이터 로드
- **사용처**: CMain에서 사이드바의 "시간표" 버튼 클릭 시
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. 로그인 상태 확인
  2. 시간표 다이얼로그 표시
  3. 백그라운드 스레드로 데이터 로드

##### `private void loadScheduleData()`
- **특징**: 선택된 모드(수강신청/미리담기)의 시간표 데이터 로드
- **사용처**: showSchedule() 내부의 백그라운드 스레드에서 실행
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. 선택된 라디오 버튼으로 모드 확인
  2. SaveDAO.getLecturesByStatus() 호출
  3. 각 강의의 시간 정보 파싱 (예: "월1,2 수3,4")
  4. 시간표 그리드에 강의 배치
  5. 강의별 랜덤 색상 할당
  6. JPanel에 강의 정보 표시 (강의명, 교수명)

##### `private void handleSaveImage(ActionEvent e)`
- **특징**: 시간표를 PNG 이미지로 저장
- **사용처**: "이미지 저장" 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. JFileChooser로 저장 위치 선택
  2. BufferedImage로 시간표 패널 캡처
  3. PNG 파일로 저장
  4. 성공 시 완료 메시지, 실패 시 오류 메시지

---

### 3.8 CAdmin.java

**클래스 설명**: 관리자 기능 (강의 CRUD)을 처리하는 컨트롤러입니다.

#### 메서드 목록 (주요 메서드)

##### `CAdmin(VAdmin vAdmin, LectureDAO lectureDAO)`
- **특징**: 생성자, 관리자 화면 이벤트 리스너 등록
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: 관리자 뷰, 강의 DAO
- **반환값**: 없음
- **주요 동작**:
  1. 추가/수정/삭제/초기화 버튼 리스너 등록
  2. 학과 찾기 버튼 리스너 등록
  3. 테이블 클릭 리스너 등록 (행 선택 시 입력 폼에 자동 채우기)

##### `void showAdminDialog()`
- **특징**: 관리자 다이얼로그 표시 및 전체 강의 목록 로드
- **사용처**: CMain에서 사이드바의 "관리자" 버튼 클릭 시 (관리자만 표시됨)
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. 관리자 다이얼로그 표시
  2. refreshTable() 호출하여 전체 강의 목록 로드

##### `void refreshTable()`
- **특징**: 전체 강의 목록 새로고침
- **사용처**: 
  - 관리자 화면 열 때
  - 강의 추가/수정/삭제 후
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. LectureDAO.getAllLectures() 호출
  2. 테이블 모델 초기화
  3. 각 강의를 행으로 추가 (강의 코드, 이름, 교수, 학점, 시간, 학과 ID)

##### `private void handleAdd(ActionEvent e)`
- **특징**: 새 강의 추가 처리
- **사용처**: "추가" 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 입력 필드 값 읽기 및 검증 (모든 필드 필수)
  2. 강의 코드, 학점 숫자 변환
  3. LectureDAO.insertLecture() 호출
  4. 성공 시: 완료 메시지, 테이블 새로고침, 입력 폼 초기화
  5. 실패 시: 오류 메시지 (중복 ID, 외래키 오류 등)

##### `private void handleUpdate(ActionEvent e)`
- **특징**: 기존 강의 정보 수정 처리
- **사용처**: "수정" 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 입력 필드 값 읽기 및 검증
  2. 강의 코드, 학점 숫자 변환
  3. LectureDAO.updateLecture() 호출
  4. 성공 시: 완료 메시지, 테이블 새로고침
  5. 실패 시: 오류 메시지
- **주의사항**: 강의 코드(PK)는 수정 불가

##### `private void handleDelete(ActionEvent e)`
- **특징**: 강의 삭제 처리
- **사용처**: "삭제" 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 테이블에서 선택된 강의 확인
  2. 확인 다이얼로그 표시
  3. 사용자 확인 시 LectureDAO.deleteLecture() 호출
  4. 성공 시: 완료 메시지, 테이블 새로고침, 입력 폼 초기화
  5. 실패 시: 오류 메시지 (외래키 제약 등)

##### `private void handleShowDepartments(ActionEvent e)`
- **특징**: 전체 학과 목록 팝업 표시
- **사용처**: "학과 찾기" 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. LectureDAO.getAllDepartments() 호출
  2. VDeptList 다이얼로그 생성 및 표시
  3. 학과 목록을 리스트로 표시 (ID: 학과명 (소속 대학) 형식)

##### `private void handleClear(ActionEvent e)`
- **특징**: 입력 폼 초기화
- **사용처**: "초기화" 버튼 클릭 시
- **매개변수**: `e` - ActionEvent
- **반환값**: 없음
- **주요 동작**: 모든 입력 필드를 빈 문자열로 설정

---

### 3.9 CListController.java (추상 클래스)

**클래스 설명**: CRegister와 CPreRegister의 공통 로직을 추출한 추상 부모 클래스입니다. 템플릿 메서드 패턴을 적용했습니다.

#### 메서드 목록

##### `protected CListController(JPanel viewPanel, JTable table, MMain mMain, SaveDAO saveDAO, String status)`
- **특징**: 생성자, 공통 필드 초기화
- **사용처**: CRegister, CPreRegister의 생성자에서 super() 호출
- **매개변수**:
  - `viewPanel`: 뷰 패널
  - `table`: 강의 목록 테이블
  - `mMain`: 메인 모델
  - `saveDAO`: 저장 DAO
  - `status`: 상태 (REGISTER 또는 PRE_REGISTER)
- **반환값**: 없음

##### `void refreshTable()`
- **특징**: 강의 목록 새로고침 (템플릿 메서드)
- **사용처**: 자식 클래스에서 public으로 호출
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. 현재 사용자 ID 확인
  2. SaveDAO.getLecturesByStatus() 호출
  3. updateViewTable() 호출 (추상 메서드, 자식 클래스에서 구현)

##### `protected void handleDelete(ActionEvent event)`
- **특징**: 강의 삭제 처리 (공통 로직)
- **사용처**: 자식 클래스의 삭제 버튼 리스너에서 호출
- **매개변수**: `event` - ActionEvent
- **반환값**: 없음
- **주요 동작**:
  1. 로그인 상태 확인
  2. 테이블에서 선택된 강의 확인
  3. SaveDAO.removeLecture() 호출
  4. 성공 시: 완료 메시지 (REGISTER면 "취소", PRE_REGISTER면 "삭제"), 테이블 새로고침
  5. 실패 시: 오류 메시지

##### `protected abstract void updateViewTable(List<MLecture> lectureList)`
- **특징**: 테이블 UI 업데이트 (추상 메서드, 자식 클래스에서 구현)
- **사용처**: refreshTable() 내부에서 호출
- **매개변수**: `lectureList` - 표시할 강의 목록
- **반환값**: 없음
- **구현 필요**: 자식 클래스에서 각각의 뷰에 맞게 테이블 업데이트 로직 구현

---

### 3.10 ControllerBundle.java

**클래스 설명**: 여러 컨트롤러를 묶어서 전달하기 위한 단순 컨테이너 클래스입니다.

#### 필드 목록
- `search` (CSearch): 검색 컨트롤러
- `register` (CRegister): 수강신청 컨트롤러
- `preRegister` (CPreRegister): 미리담기 컨트롤러
- `schedule` (CSchedule): 시간표 컨트롤러
- `admin` (CAdmin): 관리자 컨트롤러

#### 메서드 목록

##### `ControllerBundle(CSearch search, CRegister register, CPreRegister preRegister, CSchedule schedule, CAdmin admin)`
- **특징**: 생성자, 모든 컨트롤러 저장
- **사용처**: ClassConnector에서 CMain 생성 시 매개변수로 전달
- **매개변수**: 각 컨트롤러 인스턴스
- **반환값**: 없음
- **목적**: 생성자 매개변수 개수를 줄여 코드 가독성 향상


---

## 4. View 패키지

View 패키지는 사용자 인터페이스를 구성하는 Swing 컴포넌트를 관리합니다. 각 View 클래스는 주로 Getter 메서드를 제공하여 Controller가 이벤트 리스너를 등록하거나 컴포넌트를 조작할 수 있게 합니다.

### 4.1 VMain.java

**클래스 설명**: 애플리케이션의 메인 JFrame으로, 모든 패널(로그인, 회원가입, 검색 등)을 담는 컨테이너 역할을 합니다.

#### 주요 컴포넌트
- `cardLayout` (CardLayout): 패널 전환용 레이아웃
- `mainPanel` (JPanel): 로그인/회원가입 패널 컨테이너
- `contentPanel` (JPanel): 메인 콘텐츠 패널 컨테이너 (검색, 수강신청, 미리담기)
- `sidebar` (JPanel): 사이드바 메뉴
- `toolbar` (JPanel): 상단 툴바 (이전, 다음, 새로고침, 로그아웃 버튼)

#### 주요 메서드

##### `VMain()`
- **특징**: 생성자, UI 컴포넌트 초기화 및 배치
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **주요 동작**:
  1. JFrame 기본 설정 (제목, 크기, 종료 동작)
  2. 툴바 생성 (네비게이션 버튼, 로그아웃 버튼)
  3. 사이드바 생성 (햄버거 메뉴, 각종 기능 버튼)
  4. CardLayout으로 패널 컨테이너 설정
  5. 초기 창 크기를 로그인 크기로 설정

##### `void addPanel(JPanel panel, String name)`
- **특징**: mainPanel(로그인/회원가입)에 패널 추가
- **사용처**: ClassConnector에서 VLogin, VSignup 추가 시
- **매개변수**:
  - `panel`: 추가할 JPanel
  - `name`: CardLayout 식별자
- **반환값**: 없음

##### `JPanel getPanel()`
- **특징**: contentPanel(메인 콘텐츠) 반환
- **사용처**: ClassConnector에서 검색/수강신청/미리담기 패널 추가 시
- **반환값**: contentPanel (JPanel)

##### `void contentPanel(String panelName)`
- **특징**: 지정된 패널로 화면 전환
- **사용처**: 
  - CMain의 네비게이션 메서드에서 패널 전환 시
  - CLogin에서 로그인 성공 후 검색 화면으로 이동 시
- **매개변수**: `panelName` - 전환할 패널의 이름
- **반환값**: 없음
- **주요 동작**: CardLayout.show()를 통해 패널 전환

##### `void toggleSidebar()`
- **특징**: 사이드바 표시/숨김 토글
- **사용처**: CMain의 햄버거 버튼 클릭 시
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**: 사이드바의 visible 속성 반전

##### `void setMainController(CMain cMain)`
- **특징**: 메인 컨트롤러 설정
- **사용처**: ClassConnector에서 CMain 생성 후 호출
- **매개변수**: `cMain` - 메인 컨트롤러
- **반환값**: 없음

##### Getter 메서드들
View 클래스는 주로 Controller가 컴포넌트에 접근할 수 있도록 Getter 메서드를 제공합니다:

- `JButton getMenuToggleButton()`: 햄버거 메뉴 버튼
- `JButton getBeforeButton()`: 이전 버튼
- `JButton getAfterButton()`: 다음 버튼
- `JButton getRefreshButton()`: 새로고침 버튼
- `JButton getLogoutButton()`: 로그아웃 버튼
- `JButton getBtnSideSearch()`: 사이드바 - 강좌 검색 버튼
- `JButton getBtnSideRegister()`: 사이드바 - 수강신청 내역 버튼
- `JButton getBtnSidePreRegister()`: 사이드바 - 미리담기 내역 버튼
- `JButton getBtnSideTimeTable()`: 사이드바 - 시간표 버튼
- `JButton getBtnSideMyInfo()`: 사이드바 - 내 정보 버튼
- `JButton getBtnSideAdmin()`: 사이드바 - 관리자 버튼
- `JButton getBtnSideTheme()`: 사이드바 - 테마 변경 버튼
- 기타 UI 컴포넌트 Getter들...

**특징**: 모든 Getter는 Controller가 리스너를 등록하거나 컴포넌트 상태를 변경할 수 있도록 제공됩니다.

---

### 4.2 VLogin.java

**클래스 설명**: 로그인 화면 UI를 구성하는 패널입니다.

#### 주요 컴포넌트
- `idField` (JTextField): 아이디 입력 필드
- `passwordField` (JPasswordField): 비밀번호 입력 필드
- `loginButton` (JButton): 로그인 버튼
- `signupButton` (JButton): 회원가입 버튼
- `resetPwButton` (JButton): 비밀번호 초기화 버튼

#### 주요 메서드

##### `VLogin()`
- **특징**: 생성자, 로그인 화면 UI 구성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **주요 동작**:
  1. GridBagLayout으로 컴포넌트 배치
  2. 라벨, 입력 필드, 버튼 생성 및 배치
  3. 각 컴포넌트에 도구 설명(ToolTip) 추가

##### `void clearFields()`
- **특징**: 입력 필드 초기화
- **사용처**: CLogin에서 로그인 성공 또는 취소 시
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**: idField와 passwordField를 빈 문자열로 설정

##### Getter 메서드들
- `JTextField getIdField()`: 아이디 입력 필드 반환
- `JPasswordField getPasswordField()`: 비밀번호 입력 필드 반환
- `JButton getLoginButton()`: 로그인 버튼 반환
- `JButton getSignupButton()`: 회원가입 버튼 반환
- `JButton getResetPwButton()`: 비밀번호 초기화 버튼 반환

---

### 4.3 VSignup.java

**클래스 설명**: 회원가입 화면 UI를 구성하는 패널입니다.

#### 주요 컴포넌트
- `nameField, idField, emailField, studentCodeField, passwordField, passwordConfirmField` (JTextField): 각종 입력 필드
- `comboCampus, comboCollege, comboDepartment` (JComboBox): 소속 선택 콤보박스
- `signupButton, cancelButton` (JButton): 회원가입/취소 버튼

#### 주요 메서드

##### `VSignup()`
- **특징**: 생성자, 회원가입 화면 UI 구성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **주요 동작**:
  1. GridBagLayout으로 입력 필드와 라벨 배치
  2. 연쇄 콤보박스 구성 (캠퍼스 → 대학 → 학과)
  3. 비밀번호 입력 필드 설정
  4. 버튼 배치

##### `void clearFields()`
- **특징**: 모든 입력 필드 초기화
- **사용처**: 회원가입 성공 또는 취소 시
- **매개변수**: 없음
- **반환값**: 없음

##### Getter 메서드들
- 각 입력 필드, 콤보박스, 버튼에 대한 Getter 제공
- 예: `JTextField getNameField()`, `JComboBox<Object> getComboCampus()` 등

---

### 4.4 VSearch.java

**클래스 설명**: 강의 검색 화면 UI를 구성하는 패널입니다.

#### 주요 컴포넌트
- `comboCollege, comboDept` (JComboBox): 대학/학과 필터 콤보박스
- `keywordField` (JTextField): 검색 키워드 입력 필드
- `searchButton` (JButton): 검색 버튼
- `registerButton, preRegisterButton` (JButton): 수강신청/미리담기 버튼
- `searchTable` (JTable): 검색 결과 테이블

#### 주요 메서드

##### `VSearch()`
- **특징**: 생성자, 검색 화면 UI 구성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **주요 동작**:
  1. 상단에 필터 패널 배치 (대학, 학과, 키워드)
  2. 중앙에 검색 결과 테이블 배치
  3. 하단에 버튼 패널 배치 (검색, 수강신청, 미리담기)

##### Getter 메서드들
- `JComboBox<Object> getComboCollege()`: 대학 콤보박스 반환
- `JComboBox<Object> getComboDept()`: 학과 콤보박스 반환
- `JTextField getKeywordField()`: 키워드 입력 필드 반환
- `JButton getSearchButton()`: 검색 버튼 반환
- `JButton getRegisterButton()`: 수강신청 버튼 반환
- `JButton getPreRegisterButton()`: 미리담기 버튼 반환
- `JTable getSearchTable()`: 검색 결과 테이블 반환

---

### 4.5 VRegister.java

**클래스 설명**: 수강신청 내역 화면 UI를 구성하는 패널입니다.

#### 주요 컴포넌트
- `registerTable` (JTable): 수강신청 목록 테이블
- `cancelButton` (JButton): 신청 취소 버튼
- `totalCreditsLabel` (JLabel): 총 학점 표시 라벨

#### 주요 메서드

##### `VRegister()`
- **특징**: 생성자, 수강신청 내역 화면 UI 구성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **주요 동작**:
  1. 중앙에 수강신청 목록 테이블 배치
  2. 하단에 총 학점 라벨 및 취소 버튼 배치

##### Getter 메서드들
- `JTable getRegisterTable()`: 수강신청 테이블 반환
- `JButton getCancelButton()`: 취소 버튼 반환
- `JLabel getTotalCreditsLabel()`: 총 학점 라벨 반환

---

### 4.6 VPreRegister.java

**클래스 설명**: 미리담기 내역 화면 UI를 구성하는 패널입니다.

#### 주요 컴포넌트
- `preRegisterTable` (JTable): 미리담기 목록 테이블
- `deleteButton` (JButton): 삭제 버튼
- `moveToRegisterButton` (JButton): 수강신청 전환 버튼
- `totalCreditsLabel` (JLabel): 총 학점 표시 라벨

#### 주요 메서드

##### `VPreRegister()`
- **특징**: 생성자, 미리담기 내역 화면 UI 구성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **주요 동작**:
  1. 중앙에 미리담기 목록 테이블 배치
  2. 하단에 총 학점 라벨 및 버튼들 배치

##### Getter 메서드들
- `JTable getPreRegisterTable()`: 미리담기 테이블 반환
- `JButton getDeleteButton()`: 삭제 버튼 반환
- `JButton getMoveToRegisterButton()`: 수강신청 전환 버튼 반환
- `JLabel getTotalCreditsLabel()`: 총 학점 라벨 반환

---

### 4.7 VSchedule.java

**클래스 설명**: 시간표 다이얼로그 UI를 구성하는 클래스입니다.

#### 주요 컴포넌트
- `scheduleDialog` (JDialog): 시간표 다이얼로그
- `registerRadio, preRegisterRadio` (JRadioButton): 모드 선택 라디오 버튼
- `saveImageButton` (JButton): 이미지 저장 버튼
- `schedulePanel` (JPanel): 시간표 그리드 패널 (9x6 GridLayout)

#### 주요 메서드

##### `VSchedule(JFrame parent)`
- **특징**: 생성자, 시간표 다이얼로그 UI 구성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: `parent` - 부모 프레임 (VMain)
- **주요 동작**:
  1. JDialog 생성 (모달 윈도우)
  2. 상단에 라디오 버튼 패널 배치 (수강신청/미리담기 선택)
  3. 중앙에 시간표 그리드 패널 배치 (월~금, 1~9교시)
  4. 하단에 이미지 저장 버튼 배치

##### `void showDialog()`
- **특징**: 시간표 다이얼로그 표시
- **사용처**: CSchedule.showSchedule()에서 호출
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**: scheduleDialog.setVisible(true) 호출

##### Getter 메서드들
- `JRadioButton getRegisterRadio()`: 수강신청 라디오 버튼 반환
- `JRadioButton getPreRegisterRadio()`: 미리담기 라디오 버튼 반환
- `JButton getSaveImageButton()`: 이미지 저장 버튼 반환
- `JPanel getSchedulePanel()`: 시간표 패널 반환

---

### 4.8 VAdmin.java

**클래스 설명**: 관리자 화면 다이얼로그 UI를 구성하는 클래스입니다.

#### 주요 컴포넌트
- `adminDialog` (JDialog): 관리자 다이얼로그
- `lectureTable` (JTable): 전체 강의 목록 테이블
- `idField, nameField, profField, creditField, timeField, deptIdField` (JTextField): 강의 정보 입력 필드
- `addButton, updateButton, deleteButton, clearButton` (JButton): CRUD 버튼
- `showDepartmentsButton` (JButton): 학과 찾기 버튼

#### 주요 메서드

##### `VAdmin(JFrame parent)`
- **특징**: 생성자, 관리자 화면 UI 구성
- **사용처**: ClassConnector에서 애플리케이션 초기화 시
- **매개변수**: `parent` - 부모 프레임 (VMain)
- **주요 동작**:
  1. JDialog 생성 (모달 윈도우)
  2. 상단에 강의 목록 테이블 배치
  3. 하단에 입력 폼 배치 (강의 정보 필드)
  4. 버튼 패널 배치 (추가, 수정, 삭제, 초기화, 학과 찾기)

##### `void showDialog()`
- **특징**: 관리자 다이얼로그 표시
- **사용처**: CAdmin.showAdminDialog()에서 호출
- **매개변수**: 없음
- **반환값**: 없음

##### Getter 메서드들
- 테이블, 입력 필드, 버튼에 대한 Getter 제공
- 예: `JTable getLectureTable()`, `JTextField getIdField()` 등

---

### 4.9 VDeptList.java

**클래스 설명**: 학과 목록을 표시하는 팝업 다이얼로그입니다.

#### 주요 컴포넌트
- `dialog` (JDialog): 학과 목록 다이얼로그
- `deptList` (JList): 학과 목록 리스트

#### 주요 메서드

##### `VDeptList(JDialog parent, List<String> departments)`
- **특징**: 생성자, 학과 목록 다이얼로그 생성 및 표시
- **사용처**: CAdmin.handleShowDepartments()에서 호출
- **매개변수**:
  - `parent`: 부모 다이얼로그
  - `departments`: 표시할 학과 목록 (String 리스트)
- **반환값**: 없음
- **주요 동작**:
  1. JDialog 생성 (모달 윈도우)
  2. JList에 학과 목록 표시
  3. JScrollPane으로 스크롤 가능하게 설정
  4. 즉시 표시 (setVisible(true))

---

## 5. Utility 및 기타

### 5.1 ConfigLoader.java

**클래스 설명**: config.properties 파일을 로드하고 전역적으로 접근할 수 있게 하는 싱글턴 클래스입니다.

#### 필드 목록
- `INSTANCE` (ConfigLoader, static final): 싱글턴 인스턴스
- `properties` (Properties): 로드된 속성 저장

#### 메서드 목록

##### `private ConfigLoader()`
- **특징**: private 생성자, 싱글턴 패턴
- **사용처**: getInstance()에서 내부적으로 호출
- **주요 동작**: load() 메서드를 호출하여 config.properties 로드

##### `static ConfigLoader getInstance()`
- **특징**: 싱글턴 인스턴스 반환
- **사용처**: 애플리케이션 전역에서 설정값 접근 시
- **매개변수**: 없음
- **반환값**: ConfigLoader 인스턴스 (INSTANCE)

##### `String getProperty(String key, String defaultValue)`
- **특징**: 설정값 조회
- **사용처**: CLogin에서 초기 비밀번호 읽기 시
- **매개변수**:
  - `key`: 속성 키
  - `defaultValue`: 기본값 (키가 없을 때 반환)
- **반환값**: 속성 값 (String)

##### `private void load()`
- **특징**: config.properties 파일 로드
- **사용처**: 생성자에서 자동 호출
- **매개변수**: 없음
- **반환값**: 없음
- **예외 처리**: IOException 발생 시 SEVERE 레벨 로그 기록

---

### 5.2 ClassConnector.java

**클래스 설명**: 애플리케이션의 모든 객체를 조립하고 의존성을 주입하는 컨테이너 클래스입니다. Spring의 ApplicationContext와 유사한 역할을 합니다.

#### 메서드 목록

##### `ClassConnector()`
- **특징**: 생성자, 모든 Model/View/Controller/DAO 인스턴스 생성 및 연결
- **사용처**: RMain.main()에서 애플리케이션 시작 시
- **주요 동작**:
  1. **Model 생성**: MMain
  2. **DAO 생성**: LectureDAO, SaveDAO, UserDAO
  3. **View 생성**: VMain, VLogin, VSignup, VSearch, VRegister, VPreRegister, VSchedule, VAdmin
  4. **View 연결**: VMain에 각 패널 추가
  5. **Controller 생성**: CSearch, CSchedule, CRegister, CPreRegister, CAdmin
  6. **ControllerBundle 생성 및 CMain 생성**
  7. **CLogin 생성 및 CSignup 생성**
  8. **순환 참조 해결**: CLogin.setCMain() 호출
- **설계 의도**: 생성 순서와 의존성을 한 곳에서 관리하여 RMain의 복잡도 감소

##### `VMain getMainView()`
- **특징**: 메인 뷰 반환
- **사용처**: RMain에서 메인 뷰를 가져와 표시
- **매개변수**: 없음
- **반환값**: VMain 인스턴스

##### `void initialize()`
- **특징**: 애플리케이션 초기화 및 시작
- **사용처**: RMain.main()에서 모든 객체 조립 후 호출
- **매개변수**: 없음
- **반환값**: 없음
- **주요 동작**:
  1. 로그인 패널로 전환
  2. 메인 뷰를 화면에 표시 (setVisible(true))

---

### 5.3 RMain.java

**클래스 설명**: 애플리케이션의 진입점(main 메서드)을 담당하는 클래스입니다.

#### 필드 목록
- `classConnector` (ClassConnector): 객체 조립 컨테이너

#### 메서드 목록

##### `RMain()`
- **특징**: 생성자, ClassConnector 인스턴스 생성
- **사용처**: main() 메서드에서 호출
- **주요 동작**: classConnector = new ClassConnector()

##### `void initialize()`
- **특징**: 애플리케이션 초기화
- **사용처**: main() 메서드에서 호출
- **주요 동작**: classConnector.initialize() 호출

##### `static void main(String[] args)`
- **특징**: 애플리케이션 진입점
- **사용처**: JVM이 애플리케이션 시작 시 호출
- **매개변수**: `args` - 명령줄 인수
- **반환값**: 없음
- **주요 동작**:
  1. UI 스케일 설정 (flatlaf.uiScale)
  2. FlatDarkLaf 테마 적용 (Look and Feel)
  3. RMain 인스턴스 생성
  4. initialize() 호출하여 애플리케이션 시작
- **예외 처리**: Look and Feel 설정 실패 시 로그 기록

---

## 6. Constants 패키지

Constants 패키지는 애플리케이션 전역에서 사용되는 상수들을 정의합니다. 모든 클래스는 final이며 private 생성자를 가져 인스턴스화를 방지합니다.

### 6.1 AppConstants.java

**클래스 설명**: 애플리케이션 전역 상수 관리 클래스입니다.

#### 상수 목록

##### 학점 관련
- `MAX_CREDITS` (int): 18 - 최대 신청 가능 학점

##### 입력 유효성 검사
- `MIN_USER_ID_LENGTH` (int): 3 - 사용자 ID 최소 길이
- `MAX_USER_ID_LENGTH` (int): 15 - 사용자 ID 최대 길이
- `MIN_PASSWORD_LENGTH` (int): 8 - 비밀번호 최소 길이
- `MAX_PASSWORD_LENGTH` (int): 20 - 비밀번호 최대 길이
- `STUDENT_CODE_LENGTH` (int): 8 - 학번 자릿수

##### 창 크기
- `LOGIN_WINDOW_WIDTH` (int): 420 - 로그인 화면 너비
- `LOGIN_WINDOW_HEIGHT` (int): 320 - 로그인 화면 높이
- `SIGNUP_WINDOW_WIDTH` (int): 800 - 회원가입 화면 너비
- `SIGNUP_WINDOW_HEIGHT` (int): 600 - 회원가입 화면 높이
- `MAIN_WINDOW_WIDTH` (int): 1280 - 메인 화면 너비
- `MAIN_WINDOW_HEIGHT` (int): 800 - 메인 화면 높이

##### UI 설정
- `UI_SCALE` (String): "1.2" - UI 스케일 배율

##### 데이터베이스 오류 코드
- `DB_SUCCESS` (int): 0 - 정상 처리 완료
- `DB_ERROR_CREDIT_EXCEEDED` (int): 1 - 학점 초과
- `DB_ERROR_DUPLICATE` (int): 2 - 중복 데이터
- `DB_ERROR_GENERAL` (int): -1 - 일반 DB 오류

##### 파일 관련
- `PNG_EXTENSION` (String): ".png" - PNG 파일 확장자
- `DEFAULT_SCHEDULE_FILENAME` (String): "내시간표.png" - 시간표 이미지 기본 파일명

---

### 6.2 StatusConstants.java

**클래스 설명**: save 테이블의 status 컬럼에 사용되는 상태 상수입니다.

#### 상수 목록
- `REGISTER` (String): "reg" - 수강신청 상태
- `PRE_REGISTER` (String): "pre" - 미리담기 상태

---

### 6.3 PanelNames.java

**클래스 설명**: CardLayout 전환 시 사용되는 패널 식별자 상수입니다.

#### 상수 목록
- `LOGIN_PANEL` (String): "loginPanel" - 로그인 패널
- `SIGNUP_PANEL` (String): "signupPanel" - 회원가입 패널
- `MAIN_CONTENT_PANEL` (String): "mainContentPanel" - 메인 콘텐츠 패널
- `SEARCH_PANEL` (String): "searchPanel" - 검색 패널
- `REGISTER_PANEL` (String): "registerPanel" - 수강신청 패널
- `PREREGISTER_PANEL` (String): "preRegisterPanel" - 미리담기 패널
- `SCHEDULE_PANEL` (String): "schedulePanel" - 시간표 패널
- `ADMIN_PANEL` (String): "adminPanel" - 관리자 패널

---

### 6.4 ViewConstants.java

**클래스 설명**: View 관련 유틸리티 메서드와 상수를 제공하는 클래스입니다.

#### 주요 메서드 (static)

##### `void showInfoMessage(Component parent, String message, String title)`
- **특징**: 정보 메시지 다이얼로그 표시
- **사용처**: Controller에서 성공 메시지 표시 시
- **매개변수**:
  - `parent`: 부모 컴포넌트
  - `message`: 메시지 내용
  - `title`: 다이얼로그 제목

##### `void showErrorMessage(Component parent, String message, String title)`
- **특징**: 오류 메시지 다이얼로그 표시
- **사용처**: Controller에서 오류 메시지 표시 시
- **매개변수**: showInfoMessage()와 동일

##### `String showInputDialog(Component parent, String message, String title)`
- **특징**: 사용자 입력 다이얼로그 표시
- **사용처**: CLogin에서 비밀번호 초기화 시 정보 입력 받기
- **매개변수**: 부모 컴포넌트, 메시지, 제목
- **반환값**: 사용자 입력값 (String, 취소 시 null)

##### `void resizeFrame(JFrame frame, int width, int height)`
- **특징**: 프레임 크기 변경 및 중앙 정렬
- **사용처**: 로그인 성공/실패 시 창 크기 조정
- **매개변수**:
  - `frame`: 크기 변경할 프레임
  - `width`: 새 너비
  - `height`: 새 높이
- **주요 동작**:
  1. setSize()로 크기 변경
  2. setLocationRelativeTo(null)로 중앙 정렬

---

### 6.5 ControllerConstants.java

**클래스 설명**: Controller에서 사용하는 메시지 및 유틸리티 메서드를 제공하는 클래스입니다.

#### 주요 상수 (메시지)
- `SUCCESS_LOGIN` (String): "로그인 성공"
- `ERROR_LOGIN_FAILED` (String): "아이디 또는 비밀번호가 일치하지 않습니다."
- `TITLE_ERROR` (String): "오류"
- `TITLE_CONFIRMATION` (String): "확인"
- 기타 다수의 메시지 상수들...

#### 주요 메서드 (static)

##### `boolean isEmpty(String str)`
- **특징**: 문자열이 null 또는 빈 문자열인지 확인
- **사용처**: 입력 검증 시
- **매개변수**: `str` - 검사할 문자열
- **반환값**: 
  - `true`: null 또는 빈 문자열
  - `false`: 유효한 문자열

##### `boolean isNumeric(String str)`
- **특징**: 문자열이 숫자로만 구성되어 있는지 확인
- **사용처**: 학번, 강의 코드 검증 시
- **매개변수**: `str` - 검사할 문자열
- **반환값**:
  - `true`: 숫자로만 구성
  - `false`: 숫자 외 문자 포함 또는 null

##### `Integer tryParseInt(String str)`
- **특징**: 문자열을 Integer로 안전하게 변환
- **사용처**: 사용자 입력값을 정수로 변환 시
- **매개변수**: `str` - 변환할 문자열
- **반환값**:
  - `Integer`: 변환 성공 시 정수 객체
  - `null`: 변환 실패 시

---

### 6.6 Messages.java

**클래스 설명**: 사용자에게 표시할 메시지 문자열을 중앙 집중 관리하는 클래스입니다.

#### 주요 상수
- 각종 성공/실패/오류 메시지 문자열
- 사용처: Controller에서 JOptionPane 표시 시
- 예: `MSG_LOGIN_SUCCESS`, `MSG_SIGNUP_COMPLETE`, `MSG_ERROR_DB_CONNECTION` 등

---

## 7. 전체 아키텍처 흐름 요약

### 7.1 애플리케이션 시작 흐름

```
1. RMain.main()
   ↓
2. FlatLaf 테마 설정
   ↓
3. ClassConnector 생성
   ↓ (내부에서 순차 생성)
4. Model (MMain)
   ↓
5. DAO (UserDAO, LectureDAO, SaveDAO)
   ↓
6. View (VMain, VLogin, VSignup, VSearch, VRegister, VPreRegister, VSchedule, VAdmin)
   ↓
7. Controller (CSearch, CRegister, CPreRegister, CSchedule, CAdmin, CMain, CLogin, CSignup)
   ↓
8. ClassConnector.initialize()
   ↓
9. VMain 표시 (로그인 화면)
```

### 7.2 로그인 → 검색 흐름

```
1. 사용자가 ID/PW 입력 후 로그인 버튼 클릭
   ↓
2. VLogin → CLogin.handleLogin()
   ↓
3. UserDAO.validateUser() (BCrypt 검증)
   ↓
4. 성공 시:
   - MMain.setCurrentUserId() 설정
   - CMain.setAdminMode() (관리자 확인)
   - CMain.resetNavigation("searchPanel")
   - CSearch.loadInitialCollegeData()
   - ViewConstants.resizeFrame() (1280x800)
   - VMain.contentPanel("searchPanel")
```

### 7.3 수강신청 흐름

```
1. 사용자가 검색 후 강의 선택, 수강신청 버튼 클릭
   ↓
2. VSearch → CSearch.handleSave(REGISTER)
   ↓
3. SaveDAO.addLecture()
   ↓ (내부)
4. SaveDAO.getTotalCredits() (학점 검사)
   ↓
5. INSERT IGNORE INTO save (중복 방지)
   ↓
6. 결과 코드 반환 (SUCCESS/CREDIT_EXCEEDED/DUPLICATE/ERROR)
   ↓
7. CSearch에서 적절한 메시지 표시
```

---

## 8. 주요 설계 패턴

### 8.1 MVC (Model-View-Controller) 패턴
- **Model**: MMain, MUser, MLecture - 데이터 저장
- **View**: VMain, VLogin, VSignup 등 - UI 표시
- **Controller**: CMain, CLogin, CSearch 등 - 이벤트 처리 및 Model-View 연결

### 8.2 DAO (Data Access Object) 패턴
- **DAO 클래스**: UserDAO, LectureDAO, SaveDAO
- **목적**: 데이터베이스 접근 로직을 비즈니스 로직에서 분리
- **장점**: DB 변경 시 DAO만 수정하면 됨

### 8.3 Template Method 패턴
- **적용**: CListController (추상 클래스)
- **구현**: CRegister, CPreRegister
- **목적**: 공통 로직(refreshTable, handleDelete)을 부모 클래스에서 관리하고, 차이나는 부분(updateViewTable)만 자식 클래스에서 구현

### 8.4 Singleton 패턴
- **적용**: ConfigLoader
- **목적**: config.properties를 전역적으로 단일 인스턴스로 관리

### 8.5 Dependency Injection 패턴
- **적용**: ClassConnector
- **목적**: 생성자를 통해 의존성 주입, 결합도 감소

---

## 9. 보안 기능 상세

### 9.1 비밀번호 보안
- **BCrypt 해싱**: 비밀번호를 평문으로 저장하지 않고 BCrypt 해시로 저장
- **자동 마이그레이션**: 평문 비밀번호 발견 시 자동으로 해시로 변환
- **메모리 보안**: char[] 배열 사용 후 즉시 0으로 덮어씀

### 9.2 SQL 인젝션 방지
- **PreparedStatement 사용**: 모든 쿼리에서 파라미터 바인딩 사용
- **동적 쿼리**: StringBuilder로 쿼리 구성 시에도 PreparedStatement 사용

### 9.3 ReDoS 공격 방지
- **미리 컴파일된 정규표현식**: Pattern 객체를 static final로 선언
- **목적**: 정규표현식 컴파일 시간 최소화 및 ReDoS 방지

---

## 10. 테스트 및 디버깅 팁

### 10.1 로그 확인
- **로거 사용**: 모든 DAO와 Controller에 java.util.logging.Logger 사용
- **로그 레벨**:
  - INFO: 정상 동작 (로그인 성공 등)
  - WARNING: 경고 (SQL 오류, 중복 검사 실패 등)
  - SEVERE: 심각한 오류 (DB 연결 실패, 드라이버 로드 실패 등)

### 10.2 DB 연결 문제 해결
- **config.properties 확인**: 프로젝트 루트에 파일 존재 및 비밀번호 정확성
- **MySQL 서버 상태**: 서버가 실행 중인지 확인
- **DB 존재 여부**: lms_data 데이터베이스 생성 확인

### 10.3 테스트 계정
- **일반 사용자**: test / 1234
- **관리자**: DB에서 role을 'admin'으로 설정

---

## 11. 향후 개선 방향

### 11.1 추가 가능한 기능
- 비밀번호 변경 UI 추가
- 이메일 인증
- 강좌 평가 시스템
- 성적 조회 기능
- 졸업 요건 확인

### 11.2 코드 품질 개선
- 단위 테스트 추가 (JUnit)
- 통합 테스트 추가
- 코드 커버리지 향상
- SonarCloud 이슈 해결

---

## 12. 참고 문서

- **README.md**: 프로젝트 전체 개요 및 설치 방법
- **JavaDoc**: 각 클래스 및 메서드의 상세 주석
- **ERD**: 데이터베이스 스키마 다이어그램

---

**문서 작성일**: 2025년
**작성자**: 프로젝트 개발팀
**버전**: 1.0

